{
  "name": "shared-store-cross-framework",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "./react-counter",
    "./shared-store",
    "./shell",
    "./vue-counter"
  ],
  "description": "This example demos a Vue3 in React application which shares the same store.",
  "scripts": {
    "start": "yarn workspaces foreach -A --parallel run start",
    "build": "yarn workspaces foreach -A run build",
    "serve": "yarn workspaces foreach -A --parallel run serve",
    "clean": "yarn workspaces foreach -A --parallel run clean",
    "e2e:ci": "yarn start & wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress-e2e/config/cypress.config.ts --config '{\"supportFile\": \"../cypress-e2e/support/e2e.ts\"}' --spec \"./e2e/tests/runAll.cy.ts\" --browser=chrome"
  },
  "devDependencies": {
    "wait-on": "7.2.0"
  },
  "packageManager": "yarn@4.3.1"
}
